org.gradle.jvmargs=-Xms1024m -Xmx4096m

# Version-agnostic CEF library (no Minecraft dependency). One build serves every MC version.
archives_base_name=mcef-nova
mod_version=1.0.0
maven_group=com.github.FPSMasterTeam

# Provided by Minecraft at runtime; used compileOnly here.
lwjgl_version=3.3.3

# Pins the vendored java-cef/ bindings to the matching native build (downloaded at runtime).
jcef_commit=9bd969a508d52a150f42ac1baebece2c7cdcf66d

